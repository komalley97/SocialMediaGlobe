/**
 * function renderMap to render gMap object with attached initial settings/styles
 * takes a map object mapobj and creates a google map on target div mapEl
 * */

export function renderMap(mapobj, mapEl) {

}